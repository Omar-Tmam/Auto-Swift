import 'package:flutter/material.dart';

class TabletLayoutAdminView extends StatelessWidget {
  const TabletLayoutAdminView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}